# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure('2') do |config|

  # Vm
  config.vm.box      = 'devbox-debian'
  config.vm.hostname = 'devbox-debian.4.vagrant.virtualbox'

  config.vm.synced_folder '..', '/srv'

end
